{
  "name": "admin-db",
  "version": "1.0.0",
  "description": "后台管理系统 - 数据库",
  "private": true,
  "scripts": {
    "migrate": "prisma migrate dev",
    "migrate:reset": "prisma migrate reset",
    "seed": "tsx seed.ts",
    "db:init": "prisma migrate dev --name init && tsx seed.ts",
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio"
  },
  "prisma": {
    "schema": "schema.prisma",
    "seed": "tsx seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^26.1.1",
    "prisma": "^6.3.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
